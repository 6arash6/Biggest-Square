# ex05: ft_putstr

**Goal:** Print a string to standard output.

- **Prototype:** `void ft_putstr(char *str);`
- **Allowed functions:** Only `write`.
- **How it works:**
  - Loops through the string, printing each character with `write`.

**Example usage:**
```c
ft_putstr("Hello"); // Output: Hello
```
